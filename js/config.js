// Shared geometry constants. Read-only for all modules; importable in Node.
export const ROOM_W = 1024;   // logical playfield width
export const ROOM_H = 640;    // logical playfield height (the canvas is exactly this)
export const FLOOR_Y = 570;   // y of the floor surface; anything at y >= FLOOR_Y is floor/baseboard
export const CEILING_Y = 0;
export const GLIDER_W = 48;
export const GLIDER_H = 22;
